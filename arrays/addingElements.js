const numbers = [3, 4];

// Métodos para arrays

// Adicionar ao final do array
numbers.push(5, 6);

// Adicionar ao começo do array
numbers.unshift(1, 2)

// Adicionar ao meio do array
numbers.splice(3, 0, 3.5, 'x', 'y');

console.log(numbers);