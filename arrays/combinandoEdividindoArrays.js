let array1 = [1, 2, 3];
let array2 = [4, 5, 6];

// .concat usado para concatenar arrays
let combined = array1.concat(array2);
console.log(combined);

// .slice divide o array a partir dos index do inicio desejado e do final
let slice = combined.slice(3, 6);
console.log(slice);